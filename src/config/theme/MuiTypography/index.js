import grey from "../UserDefinedColors/grey";

const config = {
  overrides: {
    root: {
      color: grey.default,
    },
    button: {
      "text-transform": "capitalize",
    },
    colorSecondary: {
      color: grey.A500,
    },
    colorTextSecondary: {
      color: "white",
    },
  },
};

export default config;
