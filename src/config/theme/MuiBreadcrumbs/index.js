import { grey } from "../UserDefinedColors";

const muiBreadcrumbs = {
  overrides: {
    root: {
      fontSize: "0.875rem",
    },
    separator: {
      color: grey.A400,
      fontSize: "1.2rem",
    },
    li: {
      color: grey.A450,
    },
  },
};

export default muiBreadcrumbs;
